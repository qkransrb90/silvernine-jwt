spring:

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    com.example: DEBUG

jwt:
  header: Authorization
  secret: bGthbndla2xubGthbmVsa3d0bmxrYW5la3RsbmFsa3dlbnRhYWx3amVmbGthamtsZmphZWtsZmprbGFqa3dlZmV3
  token-validity-in-seconds: 86400